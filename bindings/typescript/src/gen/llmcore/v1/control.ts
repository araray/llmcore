// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: llmcore/v1/control.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";
import { Empty } from "./common";

export const protobufPackage = "llmcore.v1";

/** llmcore.v1 — control plane: capability negotiation, health, config reload. */

export interface ProviderInfo {
  name: string;
  available: boolean;
}

/** Handshake payload used for capability negotiation (spec §15). */
export interface ServerInfo {
  llmcoreVersion: string;
  bridgeVersion: string;
  /** "llmcore.v1" */
  contractVersion: string;
  /** e.g. ["grpc", "http"] */
  transports: string[];
  providers: ProviderInfo[];
  /** e.g. ["tier0", "transport.grpc", ...] */
  capabilities: string[];
  /** e.g. ["T0"] */
  tiers: string[];
}

export interface HealthStatus {
  ok: boolean;
  detail: string;
}

export interface ReloadConfigRequest {
  path?: string | undefined;
}

export interface ReloadConfigResponse {
  ok: boolean;
}

function createBaseProviderInfo(): ProviderInfo {
  return { name: "", available: false };
}

export const ProviderInfo: MessageFns<ProviderInfo> = {
  encode(message: ProviderInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.available !== false) {
      writer.uint32(16).bool(message.available);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProviderInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProviderInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.available = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProviderInfo {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      available: isSet(object.available) ? globalThis.Boolean(object.available) : false,
    };
  },

  toJSON(message: ProviderInfo): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.available !== false) {
      obj.available = message.available;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProviderInfo>, I>>(base?: I): ProviderInfo {
    return ProviderInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProviderInfo>, I>>(object: I): ProviderInfo {
    const message = createBaseProviderInfo();
    message.name = object.name ?? "";
    message.available = object.available ?? false;
    return message;
  },
};

function createBaseServerInfo(): ServerInfo {
  return {
    llmcoreVersion: "",
    bridgeVersion: "",
    contractVersion: "",
    transports: [],
    providers: [],
    capabilities: [],
    tiers: [],
  };
}

export const ServerInfo: MessageFns<ServerInfo> = {
  encode(message: ServerInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.llmcoreVersion !== "") {
      writer.uint32(10).string(message.llmcoreVersion);
    }
    if (message.bridgeVersion !== "") {
      writer.uint32(18).string(message.bridgeVersion);
    }
    if (message.contractVersion !== "") {
      writer.uint32(26).string(message.contractVersion);
    }
    for (const v of message.transports) {
      writer.uint32(34).string(v!);
    }
    for (const v of message.providers) {
      ProviderInfo.encode(v!, writer.uint32(42).fork()).join();
    }
    for (const v of message.capabilities) {
      writer.uint32(50).string(v!);
    }
    for (const v of message.tiers) {
      writer.uint32(58).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.llmcoreVersion = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.bridgeVersion = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.contractVersion = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.transports.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.providers.push(ProviderInfo.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.capabilities.push(reader.string());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.tiers.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerInfo {
    return {
      llmcoreVersion: isSet(object.llmcoreVersion)
        ? globalThis.String(object.llmcoreVersion)
        : isSet(object.llmcore_version)
        ? globalThis.String(object.llmcore_version)
        : "",
      bridgeVersion: isSet(object.bridgeVersion)
        ? globalThis.String(object.bridgeVersion)
        : isSet(object.bridge_version)
        ? globalThis.String(object.bridge_version)
        : "",
      contractVersion: isSet(object.contractVersion)
        ? globalThis.String(object.contractVersion)
        : isSet(object.contract_version)
        ? globalThis.String(object.contract_version)
        : "",
      transports: globalThis.Array.isArray(object?.transports)
        ? object.transports.map((e: any) => globalThis.String(e))
        : [],
      providers: globalThis.Array.isArray(object?.providers)
        ? object.providers.map((e: any) => ProviderInfo.fromJSON(e))
        : [],
      capabilities: globalThis.Array.isArray(object?.capabilities)
        ? object.capabilities.map((e: any) => globalThis.String(e))
        : [],
      tiers: globalThis.Array.isArray(object?.tiers)
        ? object.tiers.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: ServerInfo): unknown {
    const obj: any = {};
    if (message.llmcoreVersion !== "") {
      obj.llmcoreVersion = message.llmcoreVersion;
    }
    if (message.bridgeVersion !== "") {
      obj.bridgeVersion = message.bridgeVersion;
    }
    if (message.contractVersion !== "") {
      obj.contractVersion = message.contractVersion;
    }
    if (message.transports?.length) {
      obj.transports = message.transports;
    }
    if (message.providers?.length) {
      obj.providers = message.providers.map((e) => ProviderInfo.toJSON(e));
    }
    if (message.capabilities?.length) {
      obj.capabilities = message.capabilities;
    }
    if (message.tiers?.length) {
      obj.tiers = message.tiers;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServerInfo>, I>>(base?: I): ServerInfo {
    return ServerInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServerInfo>, I>>(object: I): ServerInfo {
    const message = createBaseServerInfo();
    message.llmcoreVersion = object.llmcoreVersion ?? "";
    message.bridgeVersion = object.bridgeVersion ?? "";
    message.contractVersion = object.contractVersion ?? "";
    message.transports = object.transports?.map((e) => e) || [];
    message.providers = object.providers?.map((e) => ProviderInfo.fromPartial(e)) || [];
    message.capabilities = object.capabilities?.map((e) => e) || [];
    message.tiers = object.tiers?.map((e) => e) || [];
    return message;
  },
};

function createBaseHealthStatus(): HealthStatus {
  return { ok: false, detail: "" };
}

export const HealthStatus: MessageFns<HealthStatus> = {
  encode(message: HealthStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== false) {
      writer.uint32(8).bool(message.ok);
    }
    if (message.detail !== "") {
      writer.uint32(18).string(message.detail);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HealthStatus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHealthStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.detail = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HealthStatus {
    return {
      ok: isSet(object.ok) ? globalThis.Boolean(object.ok) : false,
      detail: isSet(object.detail) ? globalThis.String(object.detail) : "",
    };
  },

  toJSON(message: HealthStatus): unknown {
    const obj: any = {};
    if (message.ok !== false) {
      obj.ok = message.ok;
    }
    if (message.detail !== "") {
      obj.detail = message.detail;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HealthStatus>, I>>(base?: I): HealthStatus {
    return HealthStatus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HealthStatus>, I>>(object: I): HealthStatus {
    const message = createBaseHealthStatus();
    message.ok = object.ok ?? false;
    message.detail = object.detail ?? "";
    return message;
  },
};

function createBaseReloadConfigRequest(): ReloadConfigRequest {
  return { path: undefined };
}

export const ReloadConfigRequest: MessageFns<ReloadConfigRequest> = {
  encode(message: ReloadConfigRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.path !== undefined) {
      writer.uint32(10).string(message.path);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReloadConfigRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReloadConfigRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.path = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReloadConfigRequest {
    return { path: isSet(object.path) ? globalThis.String(object.path) : undefined };
  },

  toJSON(message: ReloadConfigRequest): unknown {
    const obj: any = {};
    if (message.path !== undefined) {
      obj.path = message.path;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReloadConfigRequest>, I>>(base?: I): ReloadConfigRequest {
    return ReloadConfigRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReloadConfigRequest>, I>>(object: I): ReloadConfigRequest {
    const message = createBaseReloadConfigRequest();
    message.path = object.path ?? undefined;
    return message;
  },
};

function createBaseReloadConfigResponse(): ReloadConfigResponse {
  return { ok: false };
}

export const ReloadConfigResponse: MessageFns<ReloadConfigResponse> = {
  encode(message: ReloadConfigResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ok !== false) {
      writer.uint32(8).bool(message.ok);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReloadConfigResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReloadConfigResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReloadConfigResponse {
    return { ok: isSet(object.ok) ? globalThis.Boolean(object.ok) : false };
  },

  toJSON(message: ReloadConfigResponse): unknown {
    const obj: any = {};
    if (message.ok !== false) {
      obj.ok = message.ok;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReloadConfigResponse>, I>>(base?: I): ReloadConfigResponse {
    return ReloadConfigResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReloadConfigResponse>, I>>(object: I): ReloadConfigResponse {
    const message = createBaseReloadConfigResponse();
    message.ok = object.ok ?? false;
    return message;
  },
};

export type ControlServiceService = typeof ControlServiceService;
export const ControlServiceService = {
  getInfo: {
    path: "/llmcore.v1.ControlService/GetInfo" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: Empty): Buffer => Buffer.from(Empty.encode(value).finish()),
    requestDeserialize: (value: Buffer): Empty => Empty.decode(value),
    responseSerialize: (value: ServerInfo): Buffer => Buffer.from(ServerInfo.encode(value).finish()),
    responseDeserialize: (value: Buffer): ServerInfo => ServerInfo.decode(value),
  },
  health: {
    path: "/llmcore.v1.ControlService/Health" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: Empty): Buffer => Buffer.from(Empty.encode(value).finish()),
    requestDeserialize: (value: Buffer): Empty => Empty.decode(value),
    responseSerialize: (value: HealthStatus): Buffer => Buffer.from(HealthStatus.encode(value).finish()),
    responseDeserialize: (value: Buffer): HealthStatus => HealthStatus.decode(value),
  },
  /** LLMCore.reload_config (api.py:541) */
  reloadConfig: {
    path: "/llmcore.v1.ControlService/ReloadConfig" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ReloadConfigRequest): Buffer => Buffer.from(ReloadConfigRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ReloadConfigRequest => ReloadConfigRequest.decode(value),
    responseSerialize: (value: ReloadConfigResponse): Buffer =>
      Buffer.from(ReloadConfigResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ReloadConfigResponse => ReloadConfigResponse.decode(value),
  },
} as const;

export interface ControlServiceServer extends UntypedServiceImplementation {
  getInfo: handleUnaryCall<Empty, ServerInfo>;
  health: handleUnaryCall<Empty, HealthStatus>;
  /** LLMCore.reload_config (api.py:541) */
  reloadConfig: handleUnaryCall<ReloadConfigRequest, ReloadConfigResponse>;
}

export interface ControlServiceClient extends Client {
  getInfo(request: Empty, callback: (error: ServiceError | null, response: ServerInfo) => void): ClientUnaryCall;
  getInfo(
    request: Empty,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ServerInfo) => void,
  ): ClientUnaryCall;
  getInfo(
    request: Empty,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ServerInfo) => void,
  ): ClientUnaryCall;
  health(request: Empty, callback: (error: ServiceError | null, response: HealthStatus) => void): ClientUnaryCall;
  health(
    request: Empty,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: HealthStatus) => void,
  ): ClientUnaryCall;
  health(
    request: Empty,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: HealthStatus) => void,
  ): ClientUnaryCall;
  /** LLMCore.reload_config (api.py:541) */
  reloadConfig(
    request: ReloadConfigRequest,
    callback: (error: ServiceError | null, response: ReloadConfigResponse) => void,
  ): ClientUnaryCall;
  reloadConfig(
    request: ReloadConfigRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ReloadConfigResponse) => void,
  ): ClientUnaryCall;
  reloadConfig(
    request: ReloadConfigRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ReloadConfigResponse) => void,
  ): ClientUnaryCall;
}

export const ControlServiceClient = makeGenericClientConstructor(
  ControlServiceService,
  "llmcore.v1.ControlService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): ControlServiceClient;
  service: typeof ControlServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
